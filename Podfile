# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

inhibit_all_warnings!

target 'TestTMDB' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestTMDB
  pod 'Alamofire', '~> 4.8.0'
  pod 'Kingfisher', '~> 4.10.1'
  pod 'Wormholy', '~> 1.4.0', configurations: ['Debug']
  pod 'AlamofireNetworkActivityLogger', '~> 2.4.0m', configurations: ['Debug']

  pod 'SVProgressHUD', '~> 2.2.5'
  pod 'EZAlertController', :git => 'https://github.com/thellimist/EZAlertController.git'

  pod 'SwifterSwift', '~> 4.6.0'
  pod 'SwiftyJSON', '~> 5.0.0'

  pod 'YouTubePlayer'

  target 'TestTMDBTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestTMDBUITests' do
    # Pods for testing
  end

end
